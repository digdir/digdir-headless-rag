## Summary

<!-- One or two sentences: what does this PR do, and why? -->

## Changes

<!-- Bullet list of the concrete changes. Call out anything a reviewer might not
     expect from the summary alone (renames, deleted files, config changes). -->

-

## Test plan

<!-- How did you verify this? Commands run, manual steps taken, screenshots for
     UI changes. "Ran bb lint / bb test-config / bb test" is a floor, not a plan —
     say what you did to confirm the actual behavior changed as intended. -->

-

## Risk / rollback

<!-- What's the blast radius if this is wrong? How would we roll it back
     (revert commit, feature flag, config change)? For docs-only changes, say so
     explicitly and skip the rest of this section. -->
