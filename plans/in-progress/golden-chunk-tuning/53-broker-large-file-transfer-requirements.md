# Q53 v1 — What does Altinn Broker need to handle to move large files reliably and securely between senders and recipients?

**Grounded source**: `ae6e914f9ecd` (Capabilities, technology). **Register**: developer — frames around concrete needs ("move large files reliably and securely") rather than corpus "value stream stages", "system capabilities", "non-functional requirements".

## Goldens (read-confirmed)
- `826281f6dacf` — core; enumerates the requirements (performance/large files, scalability, availability, encryption in transit and at rest, access control, audits).
- `fb9f9b251b38` — supporting; high-level user needs (sender sends large files to recipients; service owner facilitates secure transfer).
- `719d88108597` — supporting; value stream model and the user types (senders, recipients, service owners).

## Cited chunks

`826281f6dacf` `fb9f9b251b38` `719d88108597`
