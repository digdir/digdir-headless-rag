# Q20 v1 — How do I configure layout sets and the page order for an app with multiple pages?

**Grounded source**: `d8e7dc7970dc` (Pages, altinn-studio). **Register**: aligned (control) — deliberately uses corpus surface terms "layout sets" and "page order".

## Goldens (read-confirmed)
- `7df452fd81ec` — core: shows the `layouts` folder structure, `layout-sets.json` mapping each layout set to a process task, and the `Settings.json` `pages` object (order, excludeFromPdf, showProgress, etc.).
- `736efe1d44d4` — supporting: intro stating multiple layout pages can be set up, with setup vs. manual settings split.

## Cited chunks

`7df452fd81ec` `736efe1d44d4`
