# Q33 v1 — how do I set my own email and mobile number to get alerts for a company, and limit it to certain services?

**Grounded source**: `920bb34a3534` (Personal notification addresses, profile reference). **Register**: lay — user says "get alerts" and "my own email and mobile number" instead of "personal notification addresses" / "resourceIncludeList".

## Goldens (read-confirmed)
- `645b0965021b` — core: explains users can configure personal notifications on behalf of an organization (own phone or company email, one of each) and restrict them to selected services via `resourceIncludeList`.
- `e2d40932a712` — supporting: shows the address model (emailAddress, phoneNumber, resourceIncludeList, partyUuid) and that a logged-in user with userId is required.

## Cited chunks

`645b0965021b` `e2d40932a712`
