# Q60 v1 — What components does the Altinn Platform provide for apps, such as authentication, authorization, events, notifications and storage?

**Grounded source**: `1c0a50be8fcd` (Altinn platform, technology). **Register**: aligned — corpus-aligned control query naming the platform components directly, matching reference-doc surface vocabulary.

## Goldens (read-confirmed)
- `e5dbdb5635a8` — core; enumerates the platform components (authentication via OIDC, authorization via ABAC/XACML 3.0, events, notifications, PDF, profile, receipt, storage).
- `505009f0b6ee` — supporting; platform components are hosted in scalable cloud infrastructure and designed as microservices.

## Cited chunks

`e5dbdb5635a8` `505009f0b6ee`
