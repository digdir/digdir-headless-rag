# Q10 v1 — How do I stop a program I set up from having access to my company's data?

**Grounded source**: `5fef20617ba6` (Delete System User, authorization). **Register**: lay — frames it as "stop a program from having access" rather than the corpus phrasing "delete the system user" / "system access" / role "Access Manager".

## Goldens (read-confirmed)
- `9c69cb9df661` — ch0: explains that only the end user with the Access Manager role can delete the system user, that deleting it automatically removes all associated delegations (i.e. revokes the access), and that it is done in the Altinn portal. Core.
- `6ec10f61e0dd` — ch1: step-by-step to log in to the portal, open the system accesses overview, select the access and click delete; notes any clients must be deleted first. Core.

## Cited chunks

`9c69cb9df661` `6ec10f61e0dd`
